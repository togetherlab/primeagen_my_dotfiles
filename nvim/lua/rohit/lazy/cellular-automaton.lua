return {
    {
        "eandrju/cellular-automaton.nvim",
        config = function()
            -- The plugin is configured in remap.lua with the <leader>ca keybinding
            -- This ensures it gets loaded properly
        end,
    },
}
